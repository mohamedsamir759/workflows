name: my First workflow
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:

jobs:
  hello:
    runs_on: ubuntu_latest
    steps:
      - uses: actions/checkout@v2
        name: hello world
        run: echo "hello world"
        shell: bash

  goodbye:
    runs_on: ubuntu_latest
    steps:
        name: good bye
        run: echo "Good bye"
        shell: bash

